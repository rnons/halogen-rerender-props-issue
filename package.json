{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "spago bundle-app -t output/bundle.js --purs-args '+RTS -N2 -RTS' && parcel build --public-url . -d docs index.html",
    "pscid:build": "spago build",
    "start": "parcel index.html"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "tailwindcss": "^1.1.3"
  }
}
